# TextFX Fixtures

- `typex-compatible-page.json` exercises the TypeX MVP page-box schema and includes unsupported fields that must be ignored on save.
- `typex-compatible-presets.json` exercises the project preset schema.
