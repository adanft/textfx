# TextFX Fixtures

- `typex-compatible-page.json` exercises a legacy page-box fixture and includes unsupported fields that must be ignored on save.
- `typex-compatible-presets.json` exercises the project preset schema.
